# 🗄️ MrCorrect ERP - Database Schema & Technical Dictionary (`YAZDSEPAR1405`)

> **توجه برای AI Agentها**: این سند شامل ساختار کامل جدول‌های کلیدی دیتابیس `YAZDSEPAR1405` (SQL Server)، روابط، کلیدها، انواع داده و قوانین تجاری فیلدها می‌باشد. برای جلوگیری از ارسال کوئری‌های اضافی سنگین یا خواندن کدهای C#، مستقیما به این سند مراجعه کنید.

---

## 1. قوانین کلی و فرمت‌های کلیدی دیتابیس (Database Conventions)

1. **فرمت کد حساب‌های ترکیبی (`HES`)**:
   - فرمت: `N_KOL-NUMBER-TNUMBER[-TNUMBER2-TNUMBER3-TNUMBER4]`
   - نمونه کد حساب شخص/مشتری: `"115-1-25-3-4-6"`
   - جداکننده: خط تیره `-`
   - سطوح خالی در حساب‌های ترکیبی حذف می‌شوند (صفر قرار داده نمی‌شود).

2. **فرمت تاریخ‌ها (`DATE_N`, `DATE_S`, `CDDATE`)**:
   - نوع داده: `bigint` یا `bigint` عددی بدون اسلش (مثال: `14040507`).

3. **ارتباط اسناد فاکتور و اسناد حسابداری**:
   - `HEAD_LST` با کلید مرکب (`NUMBER`, `TAG`) مشخص می‌شود.
   - لینک سند حسابداری به فاکتور: `HEAD_LST.N_S` -> `DEED_HED.N_S`
   - لینک اقلام سند حسابداری به فاکتور: `DEED_DTL.(NUMBER, TAG)` -> `HEAD_LST.(NUMBER, TAG)`

---

## 2. جدول هدر اسناد و فاکتورها (`HEAD_LST`)
**توضیحات**: سربرگ فاکتورهای خرید، فروش، حواله‌ها و رسیدهای انبار.

- **Primary Key**: (`NUMBER`, `TAG`)
- **کلیدهای خارجی (Foreign Keys)**:
  - `CUST_KIND` -> `CUSTKIND(CUST_COD)`
  - `N_S` -> `DEED_HED(N_S)` (سند حسابداری)
  - `DEPATMAN` -> `DEPART(DEPATMAN)`
  - `SHIFT` -> `SHIFT(SHIFT_ID)`

### کدهای نوع سند (`TAG`):
- `1`: رسید خرید
- `2`: حواله فروش
- `3`: برگشت خرید عادی
- `4`: برگشت فروش
- `24`: سایر رسیدها
- `26`: سایر حواله‌ها
- `27`: برگشت خرید آزاد

### ستون‌های کلیدی `HEAD_LST`:
| نام ستون | نوع داده | توضیحات / کاربرد |
| :--- | :--- | :--- |
| `NUMBER` | `float` (PK) | شماره سند / فاکتور |
| `TAG` | `float` (PK) | کد نوع سند (مطابق لیست فوق) |
| `ANBAR` | `int` | کد انبار اصلی |
| `DATE_N` | `bigint` | تاریخ سند به صورت عددی شمسی (مثال: `14040507`) |
| `TAH` | `nvarchar(100)` | تحویل‌دهنده / شرح کلی سند |
| `CUST_NO` | `nvarchar(40)` | کد مشتری یا تامین‌کننده |
| `MAS` | `float` | مبلغ کل سند (بدون مالیات و عوارض) |
| `VAS` | `float` | مبلغ مالیات بر ارزش افزوده |
| `TAKHFIF` | `float` | مبلغ تخفیف کل فاکتور |
| `M_NAGHD` | `float` | مبلغ پرداختی/دریافتی نقدی |
| `MABL_VAR` | `float` | مبلغ واریزی به حساب |
| `MABL_HAV` | `float` | مبلغ حواله |
| `MABL_HAZ` | `float` | مبلغ هزینه |
| `N_S` | `float` | سریال سند حسابداری متناظر (`DEED_HED.N_S`) |
| `USER_NAME` | `nvarchar(40)` | نام کاربری ثبت‌کننده سند |
| `OKF` | `bit` | وضعیت تایید نهایی سند |
| `CRT` | `datetime` | زمان و تاریخ ثبت رکورد |

---

## 3. جدول جزئیات فاکتور و اقلام انبار (`INVO_LST`)
**توضیحات**: آرتیکل‌ها و اقلام ردیف فاکتورها و اسناد انبار.

- **Primary Key**: `id` (`bigint identity`)
- **Foreign Keys**:
  - (`NUMBER`, `TAG`) -> `HEAD_LST(NUMBER, TAG)` (Cascade Update)
  - (`CODE`, `ANBAR`) -> `STUF_FSK(CODE, ANBAR)` (کالا در انبار)
  - `VAHED_K` -> `TCOD_VAHEDS(CODE)` (واحد اندازه‌گیری)

### ستون‌های کلیدی `INVO_LST`:
| نام ستون | نوع داده | توضیحات / کاربرد |
| :--- | :--- | :--- |
| `id` | `bigint` (PK) | شناسه منحصر‌به‌فرد آرتیکل |
| `NUMBER` | `float` | شماره فاکتور (لینک به `HEAD_LST`) |
| `TAG` | `float` | نوع سند (لینک به `HEAD_LST`) |
| `ANBAR` | `int` | کد انبار مربوطه |
| `RADIF` | `float` | شماره ردیف اقلام |
| `CODE` | `nvarchar(15)` | کد کالا (لینک به `STUF_DEF`) |
| `MEGH` | `float` | مقدار/تعداد اصلی کالا |
| `MEGHk` | `float` | مقدار در واحد فرعی |
| `MABL` | `float` | فی / قیمت واحد کالا |
| `MABL_K` | `float` | مبلغ کل ردیف (`MEGH * MABL`) |
| `N_RASID` | `nvarchar(40)` | شرح اقلام / توضیحات ردیف |
| `N_KOL` | `float` | کد حساب کل مرتبط |
| `N_MOIN` | `float` | کد حساب معین مرتبط |
| `N_TAF` | `float` | کد حساب تفصیلی مرتبط |
| `VISITOR` | `nvarchar(40)` | کد/نام ویزیتور فروشنده |

---

## 4. اسناد حسابداری (`DEED_HED` و `DEED_DTL`)

### 4.1. هدر اسناد حسابداری (`DEED_HED`)
- **Primary Key**: `N_S` (`float`)
- **Identity Column**: `base` (`int identity`) - شماره پایه ثابت غیرقابل تغییر برای امور مالیاتی.

**کدهای نوع سند حسابداری (`NO_S`)**:
- `0`: عمومی / نامشخص | `1`: فاکتور خرید | `2`: فاکتور فروش | `3`: برگشت خرید | `4`: برگشت فروش
- `5`: خزانه‌داری | `6`: وصول چک دریافتی | `7`: وصول چک پرداختی | `8`: حواله خروج مواد | `9`: ورود کالا به انبار
- `10`: حواله بین انبارها | `11`: حقوق و دستمزد | `12`: حواله خروج سایر | `17`: انبارگردانی

**تاییدات سند**:
- `OKF`: تایید عملیاتی جاری سند (اکثرا 1).
- `GHATEI`: قطعی شدن نهایی (غیرقابل ویرایش و حذف).
- `SGN1` تا `SGN3`: امضاهای تنظیم‌کننده، مدیر مالی و مدیرعامل.

### 4.2. ردیف‌های سند حسابداری (`DEED_DTL`)
- **Primary Key**: `id` (`bigint identity`) - منبع اصلی ترتیب آرتیکل‌ها (`RADIF` در ۹۴٪ موارد نال است).
- **Foreign Keys**:
  - `N_S` -> `DEED_HED(N_S)`
  - (`HES_K`, `HES_M`, `HES_T`) -> `TDETA_HES(N_KOL, NUMBER, TNUMBER)`
  - (`NUMBER`, `TAG`) -> `HEAD_LST(NUMBER, TAG)`
  - `BANK` -> `TCOD_BANKS(CODE)`

| نام ستون | نوع داده | توضیحات / کاربرد |
| :--- | :--- | :--- |
| `id` | `bigint` (PK) | شناسه خودکار آرتیکل |
| `N_S` | `float` | شماره سند حسابداری |
| `HES_K` | `int` | کد حساب کل |
| `HES_M` | `int` | کد حساب معین |
| `HES_T` | `int` | کد حساب تفصیلی سطح ۱ |
| `HES_T2`..`HES_T4` | `int` | کد حساب تفصیلی سطح ۲ تا ۴ |
| `HES` | `nvarchar(40)` | کد ترکیبی حساب (نمونه: `"115-1-25-3-4-6"`) |
| `SHARH` | `nvarchar(255)`| شرح آرتیکل سند |
| `BED` | `float` | مبلغ بدهکار (پیش‌فرض 0) |
| `BES` | `float` | مبلغ بستانکار (پیش‌فرض 0) |
| `N_SERI` | `float` | شماره سریال چک (در صورت مرتبط بودن با چک) |
| `BANK` | `int` | کد بانک متصل |

---

## 5. چارت حساب‌ها و کدینگ مالی (`TOTA_HES`, `DETA_HES`, `TDETA_HES`)

### 5.1. ساختار درختی حساب‌ها (Hierarchy):
`TOTA_HES (کل) -> DETA_HES (معین) -> TDETA_HES (تفصیلی 1) -> TDETA_HES2 -> TDETA_HES3 -> TDETA_HES4`

1. **`TOTA_HES` (حساب‌های کل)**:
   - PK: `NUMBER` (`int`)
   - ستون‌ها: `NAME` (نام)، `GROUP` (گروه حساب)، `NO_HES` (نوع حساب)، `M_D` (ماهیت بدهکار/بستانکار).

2. **`DETA_HES` (حساب‌های معین)**:
   - PK المركب: (`N_KOL`, `NUMBER`)
   - FK: `N_KOL` -> `TOTA_HES(NUMBER)`
   - ستون‌ها: `NAME` (نام معین)، `CODE_E` (کد اقتصادی/شناسه ملی)، `BED_BES` (مانده).

3. **`TDETA_HES` (تفصیلی سطح 1 - مشتریان / اشخاص / اشخاص حقوقی)**:
   - PK المركب: (`N_KOL`, `NUMBER`, `TNUMBER`)
   - FK: (`N_KOL`, `NUMBER`) -> `DETA_HES(N_KOL, NUMBER)`
   - ستون‌ها: `NAME`, `ECODE` (کد اقتصادی), `MCODEM` (کد ملی), `MOBILE`, `ADDRESS`, `TEL`, `CUST_COD` (نوع مشتری), `tob` (1: حقیقی، 2: حقوقی), `Longitude`, `Latitude`.

4. **`TDETA_HES2` تا `TDETA_HES4` (تفصیلی‌های سطوح ۲ تا ۴)**:
   - نگهداری حساب‌های تفصیلی لایه‌های عمیق‌تر با افزودن کلیدهای `TNUMBER2`, `TNUMBER3`, `TNUMBER4`.

---

## 6. تعریف کالا، انبار و قیمت‌گذاری (`STUF_DEF` & `STUF_FSK`)

### 6.1. `STUF_DEF` (کدینگ کالاها):
- **PK**: `CODE` (`nvarchar(15)`)
- `NAME`: نام کالا
- `N_FANI`: شماره فنی / پارت نامبر
- `VAHED`: واحد اندازه‌گیری اصلی (`TCOD_VAHEDS.CODE`)
- `B_SEF`: قیمت خرده‌فروشی | `MABL_F`: قیمت عمده‌فروشی
- `N_SEF`: نقطه سفارش | `MIN_M` / `MAX_M`: حد موجودی
- `CMBAA`: مشمول ارزش افزوده (1 یا 0) | `vra`: درصد مالیات (پیش‌فرض 9٪)
- `sstid`: شناسه کالا در سامانه مودیان | `BARCODE`: بارکد کالا

### 6.2. `STUF_FSK` (موجودی اولیه و انبار کالا):
- **PK المركب**: (`CODE`, `ANBAR`)
- `MOGODI_A`: موجودی اولیه | `FI_A`: فی موجودی اولیه | `MABL_A`: مبلغ موجودی اولیه
- `POSITION`: موقعیت قفسه/مکان فیزیکی در انبار

### 6.3. `MODULE_D` (ضریب تبدیل واحدهای فرعی):
- **PK المركب**: (`CODE`, `VAHED`)
- `NESBAT`: نسبت تبدیل به واحد پایه اصلی (پیش‌فرض 1).

### 6.4. `TAKHPERS` (درصد و تخفیفات کالا/مشتری):
- **PK المركب**: (`TAKH_COD`, `CUST_CO`)
- `TAFPER`: درصد تخفیف مصوب | `PRICE_M`: مبلغ تخفیف ثابت.

---

## 7. خزانه‌داری و مدیریت چک‌ها (`PAY_GETD` & `PAY_GETP`)

### 7.1. `PAY_GETD` (چک‌های دریافتی):
- `N_SERI`: شماره سریال چک
- `BANK`: کد بانک (`TCOD_BANKS.CODE`)
- `DATE_S`: تاریخ سررسید (عددی شمسی) | `DATE`: تاریخ دریافت
- `MABL`: مبلغ چک
- `SAYADI`: شماره صیادی ۱۶ رقمی
- `HES1`: حساب واگذاری | `HES2`: حساب برگشتی | `HES3`: حساب وصولی
- `NAME_TAH`: نام صادرکننده/پرداخت‌کننده

### 7.2. `PAY_GETP` (چک‌های پرداختی):
- ساختار مشابه `PAY_GETD` برای چک‌های صادره به نام ذینفعان و تامین‌کنندگان.

---

## 8. سطح دسترسی کاربران (`SAL_CHEK`)
- **PK المركب**: (`USERCO`, `OBJECT`)
- `USERCO`: کد کاربر (`SALA_DTL.IDD`)
- `OBJECT`: کد فرم یا اکشن (`TFORMS.IDH`)
- `RUN` (اجرا)، `SEE` (مشاهده)، `INP` (درج جدید)، `UPD` (ویرایش)، `DEL` (حذف).
