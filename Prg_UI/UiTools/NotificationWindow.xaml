<Window x:Class="UiTools.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UiTools"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        AllowsTransparency="True" 
        Background="Transparent" 
        WindowStyle="None" 
        ShowInTaskbar="False" 
        Topmost="True" 
        Width="350" 
        Height="100"
        Opacity="100" FontFamily="{StaticResource IRANYEKAN}">
    <Border Background="#FF333333" CornerRadius="10" Padding="10">
        <Grid Margin="0,0,0,-5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <materialDesign:PackIcon x:Name="NotificationIcon"
                                 Kind="InformationOutline"
                                 Foreground="White"
                                 Width="32"
                                 Height="32"
                                 VerticalAlignment="Center"/>
            <StackPanel x:Name="MATN_PANEL" Grid.Column="1" Margin="6,3,29,0" FlowDirection="RightToLeft" PreviewMouseDown="MATN_PANEL_PreviewMouseDown" Cursor="Hand" >
                <TextBlock x:Name="NotificationTitle" 
                       Foreground="White" 
                       FontSize="12" 
                       FontWeight="Bold"
                       TextWrapping="Wrap"/>
                <TextBlock x:Name="NotificationMessage" 
                       Foreground="White" 
                       FontSize="11" 
                       TextWrapping="Wrap" 
                       Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Close Button -->
            <Viewbox Margin="270,-6,-6,62" Grid.Column="1">
                <Button Style="{StaticResource MaterialDesignFlatButton}" Click="CloseButton_Click" Height="40">
                    <Button.Content>
                        <Viewbox>
                            <materialDesign:PackIcon Kind="Close" Foreground="White"/>
                        </Viewbox>
                    </Button.Content>
                </Button>
            </Viewbox>

        </Grid>
    </Border>
</Window>
