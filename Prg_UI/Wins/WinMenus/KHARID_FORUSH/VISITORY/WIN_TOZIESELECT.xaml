<Window x:Class="Prg_UI.Wins.WinMenus.KHARID_FORUSH.VISITORY.WIN_TOZIESELECT"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prg_UI.Wins.WinMenus.KHARID_FORUSH.VISITORY"
        mc:Ignorable="d"
        FontFamily="IRANYekanFN"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        xmlns:sfusion="clr-namespace:Syncfusion.UI.Xaml.Grid;assembly=Syncfusion.SfGrid.WPF" 
        xmlns:CNC="clr-namespace:Prg_UI.CUC"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Background="{x:Null}"
        AllowsTransparency="True"
        AllowDrop="True" WindowStyle="None"
        MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}"
        WindowStartupLocation="CenterScreen" Loaded="Window_Loaded" ContentRendered="Window_ContentRendered"
        Title="لیست فاکتور ها برای حواله" Height="610" Width="1024">
    <Window.Resources>
        <ItemsPanelTemplate x:Key="VSP">
            <VirtualizingStackPanel VirtualizingPanel.IsVirtualizing="True" ScrollViewer.CanContentScroll="True" VirtualizingPanel.VirtualizationMode="Recycling"/>
        </ItemsPanelTemplate>
        <SolidColorBrush x:Key="GENERAL_Grlicom" Opacity="0.3" Color="{Binding Background.Color, ElementName=GENERAL_RANG}"/>
        <!-- Style for SfDataGrid -->
        <Style x:Key="KDG"  TargetType="syncfusion:SfDataGrid">

            <Setter Property="Background" Value="{StaticResource MaterialDesign.Brush.Background}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RowHeight" Value="32" />
            <Setter Property="HeaderRowHeight" Value="40" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="GridLinesVisibility" Value="Both" />
        </Style>
        <!-- Modified Column Header Style -->
        <Style x:Key="KDGHeader" TargetType="syncfusion:GridHeaderCellControl">
            <Setter Property="Background" Value="{StaticResource MaterialDesign.Brush.Background}" />
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="Auto"/>
        </Style>

        <ContextMenu x:Key="DataGridContextMenu" FontFamily="IRANYekanFN">
            <MenuItem Header="پالودن با این بخش" Click="FilterBySelection_Click">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="Filter"  FontSize="14" />
                </MenuItem.Icon>
            </MenuItem>
            <!--Filter Excluding Selection-->
            <MenuItem Header="پالودن بدون این بخش" Click="FilterExcludingSelection_Click" >
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="FilterRemove" FontSize="14" />
                </MenuItem.Icon>
            </MenuItem>
            <Separator/>
            <MenuItem Header="حذف پالودن (فیلتر)" Click="RemoveFilterSort_Click" >
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="FilterRemoveOutline" FontSize="14" />
                </MenuItem.Icon>
            </MenuItem>
            <Separator/>
        </ContextMenu>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="12"/>
    </WindowChrome.WindowChrome>

    <Grid x:Name="mygrid">
        <Button x:Name="GENERAL_RANG" Visibility="Collapsed" Margin="600,410,504,273"  Width="1"/>
        <Border Background="{StaticResource MaterialDesign.Brush.Background}" BorderBrush="{Binding Background, ElementName=GENERAL_RANG}"  CornerRadius="10,10,10,10" Padding="8" RenderTransformOrigin="0.5,0.5" BorderThickness="1">
            <Border x:Name="TitleDrawBar" Style="{StaticResource BordG}" Background="{Binding Background, ElementName=GENERAL_RANG}" Margin="-9,-9,-9,0" VerticalAlignment="Top" MinHeight="38" UseLayoutRounding="False" Height="10" MouseLeftButtonDown="TitleDrawBar_MouseLeftButtonDown">
                <Grid>
                    <Label x:Name="LABEL_HEADER" Content="لیست فاکتور ها برای حواله" Padding="21,10,10,10" Foreground="White" Margin="0,0,110,0"/>
                    <Button x:Name="Btn_Close" Margin="0,10,13,0"  BorderBrush="{x:Null}" Background="Transparent" FontSize="12"  Height="19" VerticalAlignment="Top" HorizontalAlignment="Right" Click="Btn_Close_Click"  Foreground="White" Width="24" Padding="1,-2,1,1" IsTabStop="False" >
                        <materialDesign:PackIcon Kind="WindowClose" Margin="0,0,0,0" Height="17" Width="21" Foreground="White" />
                    </Button>
                    <Button x:Name="Btn_Minimize" Click="Btn_Minimize_Click" Margin="0,10,70,0"  BorderBrush="{x:Null}" Background="Transparent" FontSize="12"  Height="19" VerticalAlignment="Top" HorizontalAlignment="Right"  Foreground="White" Width="24" Padding="1,-2,1,1" IsTabStop="False" >
                        <materialDesign:PackIcon Kind="WindowMinimize" Margin="0,4,0,0" Height="17" Width="21" Foreground="White" />
                    </Button>
                    <Button x:Name="Btn_Max" Click="Btn_Max_Click" Margin="0,10,41,0"  BorderBrush="{x:Null}" Background="Transparent" FontSize="12"  Height="19" VerticalAlignment="Top" HorizontalAlignment="Right"  Foreground="White" Width="24" Padding="1,-2,1,1" IsTabStop="False" >
                        <materialDesign:PackIcon Kind="WindowMaximize" Margin="0,0,0,0" Height="17" Width="21" Foreground="White" />
                    </Button>
                </Grid>
            </Border>
        </Border>
        <Popup  x:Name="Pop1" AllowsTransparency="True" PopupAnimation="Fade" Placement="Relative"  Focusable="False" PlacementTarget="{Binding ElementName=TitleDrawBar}" StaysOpen="True">
            <StackPanel  Width="{Binding ActualWidth, ElementName=mygrid, Mode=OneWay}">
                <Border x:Name="Pop_Border1" CornerRadius="7" Background="#F20584AA" Height="84">
                    <TextBox Padding="10" IsReadOnly="True" x:Name="Pop1Text1"   VerticalAlignment="Center" Background="{x:Null}"
                                                Text="محل نمایش اعلان های سیستم" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="auto" Width="auto" BorderBrush="{x:Null}" SelectionBrush="{x:Null}" HorizontalAlignment="Center" Foreground="White"/>
                </Border>
            </StackPanel>
        </Popup>
        <!--HEADER↑-->
        <Grid Margin="0,38,0,0">
            <TabPanel FlowDirection="RightToLeft" Margin="5">
                <DockPanel>
                    <Label Content="محدوده تاریخی:" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                </DockPanel>
                <DockPanel>
                    <Label Content="از تاریخ :" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Margin="5"/>
                    <xctk:MaskedTextBox x:Name="AZ_NUMBER" FlowDirection="LeftToRight" Text="10000101" PreviewLostKeyboardFocus="AZ_NUMBER_PreviewLostKeyboardFocus"  Mask="0000/00/00"  Style="{StaticResource FuzzyOut}" Padding="3" Height="30" BorderThickness="1" Width="88"/>
                    <Label Content="تا تاریخ :" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Margin="5"/>
                    <xctk:MaskedTextBox x:Name="TA_NUMBER" FlowDirection="LeftToRight" PreviewLostKeyboardFocus="TA_NUMBER_PreviewLostKeyboardFocus"  Text="99991229" Mask="0000/00/00"  Style="{StaticResource FuzzyOut}" Padding="3" Height="30" BorderThickness="1" Width="88"/>
                    <Button Click="Button_Click_1" Width="300" Content="بارگذاری مجدد" FontSize="12" Margin="8,-1,0,0" Style="{StaticResource MaterialDesignFlatButton}" />
                    <Button Click="Button_Click" Width="300" Content="اتخاب همه آیتم های موجود" FontSize="12" Margin="8,-1,0,0" Style="{StaticResource MaterialDesignFlatButton}" />

                </DockPanel>
            </TabPanel>

            <syncfusion:SfDataGrid x:Name="SYNCFUSION_DG" Margin="5,52,5,98"
                     ContextMenu="{StaticResource DataGridContextMenu}"
                     ItemsSource="{Binding FACTOR_DATA}" 
                     AutoGenerateColumns="False"
                     EnableDataVirtualization="True"
                     SelectionUnit="Row"
                     AllowEditing="True"
                     AllowDraggingColumns="True"
                     AllowResizingColumns="True"
                     AllowFiltering="True"
                     AllowSorting="True"
                     SelectionMode="Extended"
                     RowHeight="30"
                     FrozenColumnCount="0"
                     FlowDirection="RightToLeft"
                     d:ItemsSource="{d:SampleData ItemCount=5}"
                     Style="{StaticResource KDG}"
                     HeaderStyle="{StaticResource KDGHeader}"
                     CurrentCellActivated="SYNCFUSION_DG_CurrentCellActivated"
                     PreviewMouseRightButtonDown="SYNCFUSION_DG_PreviewMouseRightButtonDown"
                     AllowRowHoverHighlighting="True" AllowSelectionOnPointerPressed="True" ScrollMode="Async" ShowRowHeader="True" ShowGroupDropArea="True" SelectionChanged="SYNCFUSION_DG_SelectionChanged" >
                <syncfusion:SfDataGrid.Resources>
                    <ResourceDictionary>
                        <Style TargetType="TextBox">
                            <Setter Property="AcceptsReturn" Value="True" />
                            <Setter Property="Height" Value="40" />
                        </Style>
                    </ResourceDictionary>
                </syncfusion:SfDataGrid.Resources>
                <syncfusion:SfDataGrid.Columns>
                    <syncfusion:GridCheckBoxColumn  x:Name="SELECTOR_COLUMN" HeaderText="انتخاب" AllowSorting="False" MappingName="IsSelected" AllowEditing="True" Width="80" IsHidden="False"/>
                    <syncfusion:GridTextColumn x:Name="NUMBER_HAV_COLUMN" HeaderText="شماره (حواله) فاکتور" MappingName="NUMBER" Width="160" IsReadOnly="True" />
                    <syncfusion:GridTextColumn x:Name="TARIKH_FAC_COLUMN" HeaderText="تاریخ فاکتور" MappingName="DATE_N"  Width="120" IsReadOnly="True" >
                        <syncfusion:GridTextColumn.DisplayBinding>
                            <Binding Path="DATE_N" StringFormat="####/##/##" />
                        </syncfusion:GridTextColumn.DisplayBinding>
                    </syncfusion:GridTextColumn>
                    <syncfusion:GridTextColumn x:Name="CUST_HESAB_COLUMN" HeaderText="حساب مشتری" MappingName="hes" Width="200"  IsReadOnly="True" />
                    <syncfusion:GridTextColumn x:Name="CUST_NAME_COLUMN" HeaderText="نام مشتری" MappingName="NAME" Width="350"  IsReadOnly="True" />
                    <syncfusion:GridTextColumn x:Name="MOLAH_COLUMN" HeaderText="آدرس" MappingName="ADDRESS" Width="350" IsReadOnly="True" />
                </syncfusion:SfDataGrid.Columns>
            </syncfusion:SfDataGrid>

            <TabPanel FlowDirection="RightToLeft" Height="60" VerticalAlignment="Bottom">
                <DockPanel Margin="0,10,0,-10">
                    <Label Content="تعداد رکورد ها:" Margin="5" Width="84"/>
                    <TextBlock x:Name="ROWCOUNT_TEXTBLK" Text="0" Width="230" Height="23" />
                    <TextBlock Text="{Binding SelectedItemsCount, StringFormat='تعداد انتخاب شده: {0}'}" Height="27"/>
                </DockPanel>
            </TabPanel>

            <Button x:Name="BTN_SENDGRP" Click="BTN_SENDGRP_Click"  Content="اضافه کردن آیتم های انتخاب شده" Foreground="#DDFFFFFF" FontSize="12" Margin="5,0,7,57" VerticalAlignment="Bottom"/>
        </Grid>
    </Grid>
</Window>
