<Window x:Class="Prg_UI.Wins.WinMenus.ANBAR.ZF_IVO_EXTENDED"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prg_UI.Wins.WinMenus.ANBAR"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:CNC="clr-namespace:Prg_UI.CUC"
        Background="{x:Null}"
        AllowsTransparency="True"
        AllowDrop="True" WindowStyle="None"
        FontFamily="{StaticResource IRANYEKAN}"
        Title="پارامترها" Height="480" Width="1100" MinWidth="700" MinHeight="350" WindowStartupLocation="CenterScreen" WindowState="Normal"
        Loaded="Window_Loaded" PreviewKeyDown="Window_PreviewKeyDown" Closing="Window_Closing">

    <Window.Resources>
        <ItemsPanelTemplate x:Key="VSP">
            <VirtualizingStackPanel VirtualizingPanel.IsVirtualizing="True" ScrollViewer.CanContentScroll="True" VirtualizingPanel.VirtualizationMode="Recycling"/>
        </ItemsPanelTemplate>
        <SolidColorBrush x:Key="GENERAL_Grlicom" Opacity="0.3" Color="{Binding Background.Color, ElementName=GENERAL_RANG}"/>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="12"/>
    </WindowChrome.WindowChrome>

    <Grid>
        <Button x:Name="GENERAL_RANG" Visibility="Collapsed" Margin="557,410,462,353" Width="1"/>
        <Border Background="{StaticResource MaterialDesign.Brush.Background}" BorderBrush="{Binding Background, ElementName=GENERAL_RANG}" CornerRadius="10,10,10,10" Padding="8" RenderTransformOrigin="0.5,0.5" BorderThickness="1" Grid.ColumnSpan="2" Grid.RowSpan="6">
            <Border x:Name="TitleDrawBar" Style="{StaticResource BordG}" Background="{Binding Background, ElementName=GENERAL_RANG}" Margin="-9,-9,-9,0" VerticalAlignment="Top" MinHeight="38" UseLayoutRounding="False" Height="10" MouseLeftButtonDown="TitleDrawBar_MouseLeftButtonDown">
                <Grid>
                    <Label Content="پارامترها" Padding="21,10,10,10" Foreground="White" Margin="0,0,110,0"/>
                    <Button x:Name="Btn_Close" Margin="0,10,13,0" BorderBrush="{x:Null}" Background="Transparent" FontSize="12" Height="19" VerticalAlignment="Top" HorizontalAlignment="Right" Click="Btn_Close_Click" Foreground="White" Width="24" Padding="1,-2,1,1" IsTabStop="False">
                        <materialDesign:PackIcon Kind="WindowClose" Margin="0,0,0,0" Height="17" Width="21" Foreground="White"/>
                    </Button>
                    <Button x:Name="Btn_Minimize" Click="Btn_Minimize_Click" Margin="0,10,70,0" BorderBrush="{x:Null}" Background="Transparent" FontSize="12" Height="19" VerticalAlignment="Top" HorizontalAlignment="Right" Foreground="White" Width="24" Padding="1,-2,1,1" IsTabStop="False">
                        <materialDesign:PackIcon Kind="WindowMinimize" Margin="0,4,0,0" Height="17" Width="21" Foreground="White"/>
                    </Button>
                    <Button x:Name="Btn_Max" Click="Btn_Max_Click" Margin="0,10,41,0" BorderBrush="{x:Null}" Background="Transparent" FontSize="12" Height="19" VerticalAlignment="Top" HorizontalAlignment="Right" Foreground="White" Width="24" Padding="1,-2,1,1" IsTabStop="False">
                        <materialDesign:PackIcon Kind="WindowMaximize" Margin="0,0,0,0" Height="17" Width="21" Foreground="White"/>
                    </Button>
                </Grid>
            </Border>
        </Border>
        <Popup x:Name="Pop1" AllowsTransparency="True" PopupAnimation="Fade" Placement="Relative" Focusable="False" PlacementTarget="{Binding ElementName=TitleDrawBar}" StaysOpen="True">
            <StackPanel Width="{Binding ActualWidth, ElementName=mygrid, Mode=OneWay}">
                <Border x:Name="Pop_Border1" CornerRadius="7" Background="#F20584AA" Height="84">
                    <TextBox Padding="10" IsReadOnly="True" x:Name="Pop1Text1" VerticalAlignment="Center" Background="{x:Null}"
                             Text="محل نمایش اعلان های سیستم" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="auto" Width="auto" BorderBrush="{x:Null}" SelectionBrush="{x:Null}" HorizontalAlignment="Center" Foreground="White"/>
                </Border>
            </StackPanel>
        </Popup>

        <Grid x:Name="mygrid" Margin="0,45,10,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="44"/>
            </Grid.RowDefinitions>

            <DataGrid x:Name="DG_Rows"
                      Grid.Row="0"
                      Margin="5,5,5,5"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      HeadersVisibility="Column"
                      FlowDirection="RightToLeft"
                      FontSize="11"
                      RowHeight="30"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      GridLinesVisibility="All"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="چربی"         Binding="{Binding FLD1,  UpdateSourceTrigger=PropertyChanged}" Width="72"/>
                    <DataGridTextColumn Header="ماده خشک"    Binding="{Binding FLD2,  UpdateSourceTrigger=PropertyChanged}" Width="72"/>
                    <DataGridTextColumn Header="رطوبت"        Binding="{Binding FLD3,  UpdateSourceTrigger=PropertyChanged}" Width="72"/>
                    <DataGridTextColumn Header="PH"           Binding="{Binding FLD4,  UpdateSourceTrigger=PropertyChanged}" Width="60"/>
                    <DataGridTextColumn Header="نمک"          Binding="{Binding FLD5,  UpdateSourceTrigger=PropertyChanged}" Width="60"/>
                    <DataGridTextColumn Header="دانسیته"      Binding="{Binding FLD6,  UpdateSourceTrigger=PropertyChanged}" Width="72"/>
                    <DataGridTextColumn Header="پروتیین"      Binding="{Binding FLD7,  UpdateSourceTrigger=PropertyChanged}" Width="72"/>
                    <DataGridTextColumn Header="انجماد"       Binding="{Binding FLD8,  UpdateSourceTrigger=PropertyChanged}" Width="72"/>
                    <DataGridTextColumn Header="اسید"         Binding="{Binding FLD9,  UpdateSourceTrigger=PropertyChanged}" Width="60"/>
                    <DataGridTextColumn Header="الکل"         Binding="{Binding FLD10, UpdateSourceTrigger=PropertyChanged}" Width="60"/>
                    <DataGridTextColumn Header="کلی فرم"      Binding="{Binding FLD11, UpdateSourceTrigger=PropertyChanged}" Width="72"/>
                    <DataGridTextColumn Header="استاف"        Binding="{Binding FLD12, UpdateSourceTrigger=PropertyChanged}" Width="60"/>
                    <DataGridTextColumn Header="اشیرشیا"      Binding="{Binding FLD13, UpdateSourceTrigger=PropertyChanged}" Width="72"/>
                    <DataGridTextColumn Header="ذرات سوخته"   Binding="{Binding FLD14, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0">
                <Button x:Name="Btn_AddRow" Content="+ افزودن سطر" Margin="0,0,8,0"
                        BorderBrush="{x:Null}" FontSize="10" Height="32" Foreground="#DDFFFFFF" FontWeight="Bold"
                        Width="110" Padding="8,1,8,4" Click="Btn_AddRow_Click" IsTabStop="False"/>
                <Button x:Name="Btn_DelRow" Content="حذف سطر" Margin="0,0,8,0"
                        BorderBrush="{x:Null}" FontSize="10" Height="32" Foreground="#DDFFFFFF" FontWeight="Bold"
                        Width="90" Padding="8,1,8,4" Click="Btn_DelRow_Click" IsTabStop="False"/>
                <Button x:Name="Save" Content="ذخیره"
                        BorderBrush="{x:Null}" RenderTransformOrigin="0.5,0.5" FontSize="10" Height="32"
                        Foreground="#DDFFFFFF" FontWeight="Bold" Width="100" TabIndex="10"
                        Click="Save_Click" Padding="16,1,16,4"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
