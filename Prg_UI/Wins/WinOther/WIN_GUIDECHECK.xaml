<Window x:Class="Wins.WinOther.WIN_GUIDECHECK"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wins.WinOther"
        mc:Ignorable="d"
        
        xmlns:CNC="clr-namespace:Prg_UI.CUC"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" 
        MaxHeight="{Binding Height, Source={x:Static SystemParameters.WorkArea}}"
        Background="{x:Null}"
        AllowsTransparency="True"
        AllowDrop="True" WindowStyle="None"
        FontFamily="{StaticResource IRANYEKAN}"
        WindowStartupLocation="CenterScreen"
        Title="" Height="800" Width="1024" ContentRendered="Window_ContentRendered" Loaded="Window_Loaded" PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <ItemsPanelTemplate x:Key="VSP">
            <VirtualizingStackPanel VirtualizingPanel.IsVirtualizing="True" ScrollViewer.CanContentScroll="True" VirtualizingPanel.VirtualizationMode="Recycling"/>
        </ItemsPanelTemplate>
        <SolidColorBrush x:Key="GENERAL_Grlicom" Opacity="0.3" Color="{Binding Background.Color, ElementName=GENERAL_RANG}"/>

        <Style x:Key="GuideTextStyle" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource MaterialDesignBody}"/>
        </Style>

        <Style x:Key="StepNumberStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PrimaryHueMidBrush}"/>
            <Setter Property="CornerRadius" Value="50"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
        <Style x:Key="StepNumberText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="12"/>
    </WindowChrome.WindowChrome>
    <Grid x:Name="mygrid">
        <Button x:Name="GENERAL_RANG" Visibility="Collapsed" Margin="557,410,462,353"  Width="1"/>
        <Border Background="{StaticResource MaterialDesign.Brush.Background}" BorderBrush="{Binding Background, ElementName=GENERAL_RANG}"  CornerRadius="10,10,10,10" Padding="8" RenderTransformOrigin="0.5,0.5" BorderThickness="1">
            <Border x:Name="TitleDrawBar" Style="{StaticResource BordG}" Background="{Binding Background, ElementName=GENERAL_RANG}" Margin="-9,-9,-9,0" VerticalAlignment="Top" MinHeight="38" UseLayoutRounding="False" Height="10" MouseLeftButtonDown="TitleDrawBar_MouseLeftButtonDown">
                <Grid>
                    <Label Content="راهنمای چاپ چک" Padding="21,10,10,10" Foreground="White" Margin="0,0,110,0"/>
                    <Button x:Name="Btn_Close" Click="Btn_Close_Click" Margin="0,10,13,0"  BorderBrush="{x:Null}" Background="Transparent" FontSize="12"  Height="19" VerticalAlignment="Top" HorizontalAlignment="Right" Foreground="White" Width="24" Padding="1,-2,1,1" IsTabStop="False">
                        <materialDesign:PackIcon Kind="WindowClose" Margin="0,0,0,0" Height="17" Width="21" Foreground="White" />
                    </Button>
                    <Button x:Name="Btn_Max" Click="Btn_Max_Click" Margin="0,10,41,0"  BorderBrush="{x:Null}" Background="Transparent" FontSize="12"  Height="19" VerticalAlignment="Top" HorizontalAlignment="Right"  Foreground="White" Width="24" Padding="1,-2,1,1" IsTabStop="False">
                        <materialDesign:PackIcon Kind="WindowMaximize" Margin="0,0,0,0" Height="17" Width="21" Foreground="White" />
                    </Button>
                    <Button x:Name="Btn_Minimize" Click="Btn_Minimize_Click" Margin="0,10,70,0"  BorderBrush="{x:Null}" Background="Transparent" FontSize="12"  Height="19" VerticalAlignment="Top" HorizontalAlignment="Right"  Foreground="White" Width="24" Padding="1,-2,1,1" IsTabStop="False">
                        <materialDesign:PackIcon Kind="WindowMinimize" Margin="0,4,0,0" Height="17" Width="21" Foreground="White" />
                    </Button>
                </Grid>
            </Border>
        </Border>
        <Popup  x:Name="Pop1" AllowsTransparency="True" PopupAnimation="Fade" Placement="Relative"  Focusable="False" PlacementTarget="{Binding ElementName=TitleDrawBar}" StaysOpen="True">
            <StackPanel  Width="{Binding ActualWidth, ElementName=mygrid, Mode=OneWay}">
                <Border x:Name="Pop_Border1" CornerRadius="7" Background="#F20584AA" Height="84">
                    <TextBox Padding="10" IsReadOnly="True" x:Name="Pop1Text1"   VerticalAlignment="Center" Background="{x:Null}"
                                                       Text="محل نمایش اعلان های سیستم" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="auto" Width="auto" BorderBrush="{x:Null}" SelectionBrush="{x:Null}" HorizontalAlignment="Center" Foreground="White"/>
                </Border>
            </StackPanel>
        </Popup>
        <!--HEADER↑-->

        <!--Start.......................................................................................-->
        <Grid Margin="0,40,0,0" FlowDirection="RightToLeft">
            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                    <TextBlock Text="نکات مهم قبل از چاپ" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,15" Foreground="{DynamicResource PrimaryHueMidBrush}"/>


                    <materialDesign:Card Padding="10" Margin="0,0,0,10" UniformCornerRadius="8">
                        <DockPanel>
                            <Border Style="{StaticResource StepNumberStyle}">
                                <TextBlock Text="1" Style="{StaticResource StepNumberText}"/>
                            </Border>
                            <StackPanel>
                                <TextBlock Style="{StaticResource GuideTextStyle}" FontWeight="Bold" Text="تست چاپ روی کاغذ معمولی:"/>
                                <TextBlock Style="{StaticResource GuideTextStyle}" Margin="0,5,0,0" Text="یک کاغذ A4 را دقیقاً به اندازه ابعاد چک برش دهید. ابتدا روی این کاغذ تستی چاپ بگیرید و با چک اصلی مقایسه کنید تا از هدر رفتن برگ چک جلوگیری شود."/>
                            </StackPanel>
                        </DockPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Padding="10" Margin="0,0,0,10" UniformCornerRadius="8">
                        <DockPanel>
                            <Border Style="{StaticResource StepNumberStyle}">
                                <TextBlock Text="2" Style="{StaticResource StepNumberText}"/>
                            </Border>
                            <TextBlock x:Name="TXB_2" Visibility="Visible" Style="{StaticResource GuideTextStyle}" Text="برگ چک را مطابق تصویر روبرو، به صورت افقی (Landscape) در گوشه سمت راست سینی کاغذ A4 قرار دهید."/>
                        </DockPanel>
                    </materialDesign:Card>

                    <materialDesign:Card x:Name="CARD_3" Padding="10" UniformCornerRadius="8" Background="#FFF4F4F4">
                        <DockPanel>
                            <Border Style="{StaticResource StepNumberStyle}" Background="#607D8B">
                                <TextBlock Text="3" Style="{StaticResource StepNumberText}"/>
                            </Border>
                            <StackPanel>
                                <TextBlock Style="{StaticResource GuideTextStyle}" FontWeight="Bold" Text="تنظیمات درایور پرینتر (Preferences):"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <materialDesign:PackIcon Kind="FileDocumentOutline" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                    <TextBlock Text="Orientation: Portrait (عمودی)" Style="{StaticResource GuideTextStyle}" Margin="0,0,15,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <materialDesign:PackIcon Kind="RulerSquare" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                    <TextBlock Text="Size: A4" Style="{StaticResource GuideTextStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </DockPanel>
                    </materialDesign:Card>
                </StackPanel>

                <Border Grid.Column="1" Background="#FAFAFA" CornerRadius="10" BorderBrush="#EEEEEE" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="نحوه قرارگیری در پرینتر" HorizontalAlignment="Center" Margin="10" FontWeight="Bold" Opacity="0.6"/>

                        <Viewbox x:Name="VB_VERTICAL" Grid.Row="1" Margin="20" Stretch="Uniform">
                            <Canvas Width="210" Height="297" Background="White">
                                <Canvas.Effect>
                                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                                </Canvas.Effect>

                                <Rectangle Width="210" Height="297" Stroke="#CCCCCC" StrokeThickness="1" StrokeDashArray="4 2"/>
                                <TextBlock Text="سینی کاغذ (A4)" Canvas.Left="65" Canvas.Top="150" Foreground="#CCCCCC" FontSize="10" RenderTransformOrigin="0.5,0.5">
                                    <TextBlock.RenderTransform>
                                        <RotateTransform Angle="-45"/>
                                    </TextBlock.RenderTransform>
                                </TextBlock>

                                <Border Width="170" Height="85" Canvas.Top="0" BorderBrush="#7B1FA2" BorderThickness="1" Background="#F3E5F5" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Grid>
                                        <Image Source="/UiDrive/IMGS/CHECKGUIDE.jpg"  Stretch="UniformToFill" Opacity="0.9"/>

                                        <Border Background="#AA000000" VerticalAlignment="Center" HorizontalAlignment="Center" CornerRadius="4" Padding="4">
                                            <TextBlock Text="محل قرارگیری چک" Foreground="White" FontSize="10"/>
                                        </Border>
                                    </Grid>
                                </Border>

                                <Path Fill="#FF5722" Data="M 125,90 L 125,110 L 115,110 L 135,130 L 155,110 L 145,110 L 145,90 Z" Canvas.Left="0" Canvas.Top="0"/>
                                <TextBlock Text="جهت ورود به پرینتر" Canvas.Left="100" Canvas.Top="135" FontSize="8" Foreground="#FF5722"/>

                            </Canvas>
                        </Viewbox>

                        <Viewbox x:Name="VB_FITCHECKIMAGE" Visibility="Hidden" Grid.Row="1" Margin="20" Stretch="Uniform">
                            <Image Stretch="Uniform" >
                                <Image.Source>
                                    <BitmapImage DecodePixelWidth="800" DecodePixelHeight="1024" UriSource="/UiDrive/IMGS/FitCheck.png" CacheOption="OnLoad"/>
                                </Image.Source>
                            </Image>
                        </Viewbox>


                    </Grid>
                </Border>
            </Grid>
        </Grid>
      
    </Grid>
</Window>


