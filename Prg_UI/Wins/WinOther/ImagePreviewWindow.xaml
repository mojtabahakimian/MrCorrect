<Window x:Class="Prg_UI.Wins.WinOther.ImagePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prg_UI.Wins.WinOther"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="پیش نمایش تصویر" Height="600" Width="800" WindowStartupLocation="CenterScreen"
        FontFamily="{StaticResource IRANYEKAN}" FlowDirection="RightToLeft">

    <Grid Background="{StaticResource MaterialDesignPaper}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryHueMidBrush}" Padding="10">
            <DockPanel>
                <TextBlock Text="تصویر ضمیمه" Foreground="{StaticResource PrimaryHueMidForegroundBrush}" 
                           VerticalAlignment="Center" FontWeight="Bold" FontSize="16"/>
                <Button Style="{StaticResource MaterialDesignIconForegroundButton}" 
                        DockPanel.Dock="Left" HorizontalAlignment="Left" Click="CloseBtn_Click"
                        ToolTip="بستن">
                    <materialDesign:PackIcon Kind="Close" Width="24" Height="24" Foreground="{StaticResource PrimaryHueMidForegroundBrush}"/>
                </Button>
            </DockPanel>
        </Border>

        <!-- Image Viewer -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Padding="10">
            <Image x:Name="imgPreview" Stretch="Uniform" />
        </ScrollViewer>

        <!-- Footer Actions -->
        <Border Grid.Row="2" Padding="10" BorderThickness="0,1,0,0" BorderBrush="{StaticResource MaterialDesignDivider}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="بستن" Click="CloseBtn_Click" Style="{StaticResource MaterialDesignFlatButton}" Margin="5"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
