<Window x:Class="Wins.WinOther.SearchWindowCVS"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Search" Height="200" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        PreviewKeyDown="Window_PreviewKeyDown"
        Loaded="Window_Loaded"
        ContentRendered="Window_ContentRendered">
    <Border Background="White" CornerRadius="5" Padding="10" BorderBrush="Gray" BorderThickness="1">
        <StackPanel>
            <!-- Header Area -->
            <Grid MouseLeftButtonDown="TitleDrawBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Search" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="_" Click="Btn_Minimize_Click" Width="30" Margin="2"/>
                <Button Grid.Column="2" Content="□" Click="Btn_Max_Click" Width="30" Margin="2"/>
                <Button Grid.Column="3" Content="X" Click="Btn_Close_Click" Width="30" Margin="2"/>
            </Grid>
            <Separator Margin="0,5,0,5"/>
            <!-- Search Controls -->
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Column:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="ColumnComboBox" Width="120" SelectionChanged="ColumnComboBox_SelectionChanged"/>
            </StackPanel>
            <TextBox x:Name="SearchTextBox" Margin="0,5,0,5" KeyUp="SearchTextBox_KeyUp"
               PreviewGotKeyboardFocus="SearchTextBox_PreviewGotKeyboardFocus"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Clear" Click="ClearSearchButton_Click" Margin="0,0,5,0"/>
                <Button Content="Close" Click="Btn_Close_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
